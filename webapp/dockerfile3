# Use a lightweight web server image
FROM nginx:alpine

# Copy static files to the nginx web server's default location
COPY ./dist /usr/share/nginx/html

# Expose port 80 for the web server
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]

